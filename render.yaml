services:
  - type: web
    name: pipengine-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && python -m uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGO_URL
        value: mongodb+srv://matchyagear:JeepDog123!@#@pipengine.ibuatqd.mongodb.net/shadowbeta?retryWrites=true&w=majority&appName=PipEngine
      - key: DB_NAME
        value: shadowbeta
      - key: MONGODB_DISABLED
        value: false
      - key: ENVIRONMENT
        value: production
